A shader renderer similar to Shadertoy,
supporting multi-threaded CPU rendering with
the TBB library and GPU rendering based on CUDA.

Render the picture by writing the corresponding shader functions
in the `unuified_shader_entry.inc` file.

Don't forget to modify the values of
`cuda_shader_entry` and `cpu_shader_entry`.